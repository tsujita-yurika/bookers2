
  <div class = "container px-5 px-sm-0">
    <div class = "row">
      <!--左側-->
      <div class = "col-md-3">
        <h2>User info</h2>

      <%= image_tag @user.get_profile_image(100,100) %>

        <table class = "table">
          <tbody>
            <tr></tr>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
            </tr>

            <tr>
              <th>introduction</th>
              <th></th>
            </tr>

          </tbody>
        </table>

        <div class = "row">
            <%= link_to edit_user_path(@user), class: "btn btn-outline-secondary btn-block" do %>
            <i class = "fas fa-user-cog"></i>
            <% end %>
        </div>

        <h2 class = "mt-3">New book</h2>
      <%= form_with model: Book.new do |f| %>
        <h4>Title</h4>
        <%= f.text_field :title %>

        <h4>Opinion</h4>
        <%= f.text_area :body %>

        <%= f.submit class: "btn btn-success btn-sm btn-block mb-3" do %>Create Book
        <% end %>
      <% end %>
      </div>
      <!--左側　ここまで-->
      <div class = "col-md-8 offset-md-1">
        　<h2>Books</h2>
      　 <table class="table table-hover table-inverse">
       　  <thead>
       　    <tr>
        　     <th>Title</th>
        　     <th>Opinion</th>
        　     <th colspan = "3"></th>
        　   </tr>
       　  </thear>
      　   <tbody>
            <% @books.each do |book| %>
              <tr>
                <tb><%= link_to plofile_image,user_path do %></tb>
                <td><%= book.title %></td>
                <td><%= book.body %></td>
              </tr>
            <% end %>
       　  </tbody>
     　   </table>
      </div>

    </div>
  </div>